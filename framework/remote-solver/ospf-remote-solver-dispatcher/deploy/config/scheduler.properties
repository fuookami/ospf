event.adapter=inmemory
# Kafka event options (only when event.adapter=kafka)
# event.kafka.bootstrap-servers=127.0.0.1:9092
# event.kafka.client-id=remote-solver-event-port
# event.kafka.consumer-poll-interval-ms=200
# event.kafka.query-poll-timeout-ms=1500
# event.kafka.query-max-poll-rounds=8
# event.kafka.query-topics=SolvingRequest,TaskResult,SliceLifecycle,MonitorAlert

distributed-lock.adapter=inmemory
node-state.adapter=inmemory
budget.adapter=inmemory
task-state.adapter=inmemory
cost-ledger.adapter=inmemory
solver-execution.adapter=ospf
# The in-process bridge executes CP snapshots with the OSPF SCIP adapter.
# To run the same CP executor in a worker process, replace the bridge class and
# provide a command that accepts the external-process protocol documented in
# docs/remote-cp-protocol.md.
solver-execution.ospf.bridge-class=fuookami.ospf.framework.remote_solver.adapter.ospf.OspfInProcessBridge
# solver-execution.ospf.bridge-class=fuookami.ospf.framework.remote_solver.adapter.ospf.OspfExternalProcessBridge
# solver-execution.ospf.bridge.arg.command=/path/to/ospf-worker
storage.adapter=localfs
storage.localfs.root=./data/remote-solver
scheduler.loop.interval-ms=200
scheduler.performance-learning.enabled=true
scheduler.performance-learning.rate=0.2
scheduler.performance-learning.score-min=0.1
scheduler.performance-learning.score-max=10.0

# Monitor alert linkage / 监控告警联动
# monitor.alert.enabled=true
# monitor.alert.check-interval-ms=5000
# monitor.alert.overview-limit=300
# monitor.alert.cooldown-ms=60000
# monitor.alert.escalate-after-consecutive=3
# monitor.alert.stale-nodes-threshold=1
# monitor.alert.offline-nodes-threshold=1
# monitor.alert.failed-tasks-threshold=5
# monitor.alert.failed-ratio-threshold=0.3
# monitor.alert.queue-depth-threshold=100
# monitor.alert.route.event.enabled=true
# monitor.alert.route.webhook.enabled=false
# monitor.alert.route.webhook.url=http://127.0.0.1:19090/alerts
# monitor.alert.route.webhook.timeout-ms=3000

# Ktorm adapter keys (new) / Ktorm 适配器键（新）
# distributed-lock.adapter=ktorm
# distributed-lock.ktorm.url=jdbc:postgresql://127.0.0.1:5432/remote_solver
# node-state.adapter=ktorm
# node-state.ktorm.url=jdbc:postgresql://127.0.0.1:5432/remote_solver
# budget.adapter=ktorm
# budget.ktorm.url=jdbc:postgresql://127.0.0.1:5432/remote_solver
# task-state.adapter=ktorm
# task-state.ktorm.url=jdbc:postgresql://127.0.0.1:5432/remote_solver
# cost-ledger.adapter=ktorm
# cost-ledger.ktorm.url=jdbc:postgresql://127.0.0.1:5432/remote_solver
# Legacy jdbc.* keys are still accepted for compatibility / 历史 jdbc.* 键仍兼容
