//! 最小低载荷模型 / Min low payload model
/// 最小低载荷 / Min low payload (对齐 Kotlin MinLowPayload)
#[derive(Debug, Clone)]
pub struct MinLowPayload {
    /// 最小载荷 / Minimum payload
    pub min_payload: f64,
}
